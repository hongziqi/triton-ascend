# Unused. Native build is cmake -S standalone (see standalone/README.md).
#
cmake_minimum_required(VERSION 3.20)
message(FATAL_ERROR
  "Do not add this directory from a community Triton tree. "
  "Build with standalone/CMakeLists.txt (see README.md).")
