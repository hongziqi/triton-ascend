# Read the Docs configuration file
# See https://docs.readthedocs.io/en/stable/config-file/v2.html for details
#
# Dual-language (no gettext): set env READTHEDOCS_LANGUAGE per project.
# - Main project (English): Admin > Environment variables > READTHEDOCS_LANGUAGE = en
# - Translation project (Chinese): READTHEDOCS_LANGUAGE = zh_CN (or leave unset)
# Link the translation in the main project's Admin > Translations. See docs/README.md.

# Required
version: 2

# Set the OS, Python version, and other tools you might need
build:
  os: ubuntu-24.04
  tools:
    python: "3.13"

# Build documentation in the "docs/" directory with Sphinx
sphinx:
   configuration: docs/conf.py

# Optionally, but recommended,
# declare the Python requirements required to build your documentation
# See https://docs.readthedocs.io/en/stable/guides/reproducible-builds.html
python:
   install:
   - requirements: docs/requirements.txt